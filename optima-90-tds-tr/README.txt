Kermit Optima 90 Taş Polimer Kompozit Süpürgelik Technical Data Sheet HTML source.
Open index.html in a browser. Use Print > Save to PDF with A4 size and background graphics enabled.
The layout uses Kermit brand colors and Inter/Montserrat web fonts. Images are stored in the assets folder.
Feature icons note: the feature-card icons use high-DPI crops rendered from the approved PDF page, so the HTML matches the PDF visual quality instead of relying on low-resolution extracted icon bitmaps or traced SVGs.
